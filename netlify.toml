[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20.20.0"
  NPM_FLAGS = "--include=optional"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
